# MSSQL MCP Server Configuration
# Copy this file to .env.mssql and fill in your connection details
# DO NOT commit .env.mssql to git (it contains credentials)

# MSSQL Connection String Format:
# Server=<server_name_or_ip>;Database=<database_name>;User Id=<username>;Password=<password>;
# OR
# Server=<server_name_or_ip>;Database=<database_name>;Integrated Security=true;

# Example with SQL Authentication:
# MSSQL_CONNECTION_STRING=Server=localhost;Database=WileyWidget;User Id=sa;Password=YourPassword123;

# Example with Windows Authentication (Integrated Security):
# MSSQL_CONNECTION_STRING=Server=localhost;Database=WileyWidget;Integrated Security=true;

# Your connection string (Windows Authentication - recommended):
MSSQL_CONNECTION_STRING=Server=localhost;Database=WileyWidget;Integrated Security=true;
