name: Deprecated - CI Optimized (noop)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is deprecated; see .github/workflows/ci.yml"
