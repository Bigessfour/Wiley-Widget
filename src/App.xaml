<Application xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WileyWidget"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF">
    <Application.Resources>
        <!--
            Resource dictionaries are intentionally NOT merged here at XAML parse time.
            Previous runtime issues caused XAML parsing failures when some theme files
            contained color tokens that failed the XAML Color parser on certain runs.

            To make startup resilient, merged dictionaries are loaded programmatically
            from App.xaml.cs using a sanitizer/loader which validates color tokens and
            falls back safely when needed.
        -->
    </Application.Resources>
</Application>
