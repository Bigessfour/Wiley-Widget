<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Runtime optimizations for WPF startup performance -->
  <runtime>
    <!-- Disable publisher evidence generation for faster assembly loading -->
    <generatePublisherEvidence enabled="false"/>
    <!-- Assembly binding optimizations - Fixed escapes, added Syncfusion paths -->
    <assemblyBinding xmlns="urn:schemas-microsoft-com:asm.v1">
      <probing privatePath="bin;bin/plugins;lib;packages/syncfusion;lib/syncfusion"/>
    </assemblyBinding>
    <!-- GC: Server=true for Syncfusion multi-threaded rendering; concurrent=true -->
    <gcServer enabled="true"/>
    <gcConcurrent enabled="true"/>
    <gcAllowVeryLargeObjects enabled="true"/>
    <!-- Thread pool optimizations -->
    <ThreadPool_EnableWorkerTracking enabled="false"/>
    <!-- .NET 9 Performance optimizations -->
    <EnableEventPipe>false</EnableEventPipe>
    <EventPipeOutputPath></EventPipeOutputPath>
    <!-- Loader optimizations for WPF -->
    <loadFromRemoteSources enabled="true"/> <!-- Enable for dynamic themes -->
    <legacyCorruptedStateExceptionsPolicy enabled="false"/>
    <!-- Assembly loading optimizations -->
    <disableFusionUpdatesFromADManager enabled="true"/>
    <bypassTrustedAppStrongNames enabled="true"/>
  </runtime>


  <!--
  FIXED NOTES (Wiley Widget + Syncfusion):
  - Duplicates removed; escapes fixed (forward slashes).
  - GC/Server=true per Syncfusion rendering needs (https://help.syncfusion.com/wpf/grid/getting-started/virtualization).
  - AppContext: Safe subset—avoids SfDataGrid exceptions.
  - Probing: Syncfusion-inclusive (per deployment guide).
  - Network: CRL=true for secure xAI calls.
  - Test: Clean bin/obj; rebuild. Startup <1500ms expected.
  -->
</configuration>
