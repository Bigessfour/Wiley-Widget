namespace WileyWidget.WinForms;

// Form1 removed: legacy sample form no longer used. Kept intentionally empty so
// any stray references won't build accidental UI into the app. The project
// uses MainForm and code-first UserControls instead.
internal class RemovedLegacyForm1Placeholder { }
