<Page
    x:Class="WileyWidget.Uno.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20" Spacing="20">
            <TextBlock Text="Dashboard" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Welcome to Wiley Widget on Uno Platform!" 
                      Style="{StaticResource SubtitleTextBlockStyle}"/>
            
            <!-- Placeholder for dashboard widgets -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Migration Status: Initial Setup Complete" 
                          Foreground="Green" Margin="0,10,0,0"/>
                
                <Border Grid.Row="1" BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                       BorderThickness="1" Padding="20" Margin="0,10,0,0">
                    <StackPanel Spacing="10">
                        <TextBlock Text="✓ Uno Platform project created"/>
                        <TextBlock Text="✓ Prism.Uno.WinUI integrated"/>
                        <TextBlock Text="✓ Syncfusion WinUI packages added"/>
                        <TextBlock Text="⏳ ViewModels migration in progress"/>
                        <TextBlock Text="⏳ XAML views migration pending"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
