<UserControl
    x:Class="WileyWidget.WinUI.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header with Icon -->
        <Grid Grid.Row="0" Margin="24,24,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- App Icon (SVG) -->
            <Image Grid.Column="0"
                Source="ms-appx:///Assets/Icons/icon.svg"
                Width="32"
                Height="32"
                Margin="0,0,12,0"
                VerticalAlignment="Center" />

            <TextBlock Grid.Column="1"
                Text="Dashboard"
                Style="{ThemeResource TitleTextBlockStyle}"
                VerticalAlignment="Center" />
        </Grid>

        <!-- Asset Test Panel (for verification) -->
        <StackPanel Grid.Row="1" Margin="24,0,24,12" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Asset Test:" VerticalAlignment="Center" FontWeight="SemiBold" />

            <!-- Test: icon.svg -->
            <Border BorderBrush="{ThemeResource SystemAccentColor}" BorderThickness="1" Padding="8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Image Source="ms-appx:///Assets/Icons/icon.svg"
                        Width="24"
                        Height="24" />
                    <TextBlock Text="icon.svg" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Border>

            <!-- Test: icon_foreground.svg -->
            <Border BorderBrush="{ThemeResource SystemAccentColor}" BorderThickness="1" Padding="8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Image Source="ms-appx:///Assets/Icons/icon_foreground.svg"
                        Width="24"
                        Height="24" />
                    <TextBlock Text="icon_foreground.svg" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Border>

            <!-- Test: splash_screen.svg -->
            <Border BorderBrush="{ThemeResource SystemAccentColor}" BorderThickness="1" Padding="8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Image Source="ms-appx:///Assets/Splash/splash_screen.svg"
                        Width="24"
                        Height="24" />
                    <TextBlock Text="splash_screen.svg" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Data Grid -->
        <controls:DataGrid Grid.Row="2"
            ItemsSource="{Binding Metrics}"
            AutoGenerateColumns="False"
            CanUserSortColumns="True"
            IsReadOnly="True"
            GridLinesVisibility="All"
            AlternatingRowBackground="{ThemeResource SystemControlBackgroundListLowBrush}"
            CanUserReorderColumns="True"
            CanUserResizeColumns="True"
            Margin="24"
            AutomationProperties.Name="Dashboard Metrics Grid">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Metric" Binding="{Binding Name}" Width="*" />
                <controls:DataGridTextColumn Header="Value"
                    Binding="{Binding Value, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:N2}'}" Width="150" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</UserControl>
