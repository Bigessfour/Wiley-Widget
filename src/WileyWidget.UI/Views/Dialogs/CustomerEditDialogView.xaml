<UserControl x:Class="WileyWidget.Views.Dialogs.CustomerEditDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:WileyWidget.Converters"
             xmlns:behaviors="clr-namespace:WileyWidget.UI.Behaviors"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:darkTheme="clr-namespace:Syncfusion.Themes.FluentDark.WPF;assembly=Syncfusion.Themes.FluentDark.WPF"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="520" Height="640"
             MinWidth="420" MinHeight="360"
             AutomationProperties.Name="Customer Edit Dialog"
             AutomationProperties.HelpText="Dialog used for creating or editing a utility customer"
             mc:Ignorable="d"
             behaviors:FocusOnLoadBehavior.IsEnabled="True" behaviors:FocusOnLoadBehavior.TargetName="FirstFocusable">
    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <ControlTemplate x:Key="ErrorTemplate">
            <DockPanel LastChildFill="True">
                <Border BorderBrush="{DynamicResource ErrorBorderBrush}" BorderThickness="1" CornerRadius="2" DockPanel.Dock="Right"
                        Background="{DynamicResource ErrorBackgroundBrush}" Margin="5,0,0,0" Width="20" Height="20">
                    <TextBlock Text="!" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              ToolTip="{Binding ElementName=adornerPlaceholder, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}" />
                </Border>
                <AdornedElementPlaceholder x:Name="adornerPlaceholder" />
            </DockPanel>
        </ControlTemplate>
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" />
        <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SaveCommand}" />
    </UserControl.InputBindings>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
    <!-- Acrylic wrapper per FluentDark guidance; visibility bound to VM.ShowAcrylicBackground -->
    <darkTheme:SfAcrylicPanel TintBrush="#202020" TintOpacity="0.25" BlurRadius="30" NoiseOpacity="0.03"
                  Visibility="{Binding ShowAcrylicBackground, Converter={StaticResource BoolToVis}}"
                  Grid.RowSpan="3">

        <!-- SfAcrylicPanel accepts a single child; place a Grid as that child and define rows inside it -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Edit Customer" FontSize="16" FontWeight="Bold" Margin="4" AutomationProperties.Name="Edit Customer Title" />

            <ScrollViewer Grid.Row="1">
                <StackPanel Margin="4">
                <Label Content="Account Number" />
                <TextBox x:Name="FirstFocusable" Text="{Binding Customer.AccountNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Validation.ErrorTemplate="{StaticResource ErrorTemplate}" AutomationProperties.HelpText="Account number for the customer"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <Label Content="First Name" />
                <TextBox Text="{Binding Customer.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Validation.ErrorTemplate="{StaticResource ErrorTemplate}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <Label Content="Last Name" />
                <TextBox Text="{Binding Customer.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Validation.ErrorTemplate="{StaticResource ErrorTemplate}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <Label Content="Service Address" />
                <TextBox Text="{Binding Customer.ServiceAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Validation.ErrorTemplate="{StaticResource ErrorTemplate}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <Label Content="Phone" />
                <TextBox Text="{Binding Customer.PhoneNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Validation.ErrorTemplate="{StaticResource ErrorTemplate}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <Label Content="Email" />
                <TextBox Text="{Binding Customer.EmailAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Validation.ErrorTemplate="{StaticResource ErrorTemplate}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />
            </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="0,0,12,0">
                    <Border Background="{DynamicResource ErrorBackgroundBrush}" BorderBrush="{DynamicResource ErrorBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="6" Width="380" Visibility="{Binding ValidationSummary, Converter={StaticResource StringToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠" Foreground="{DynamicResource ErrorForegroundBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Top" />
                            <TextBlock Text="{Binding ValidationSummary}" Foreground="{DynamicResource ErrorForegroundBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                 <syncfusion:ButtonAdv Label="_Cancel" Command="{Binding CancelCommand}" MinWidth="100" Margin="0,0,8,0" AutomationProperties.Name="Cancel" />
                 <syncfusion:ButtonAdv Label="_Save" Command="{Binding SaveCommand}" MinWidth="100" AutomationProperties.Name="Save" />
            </StackPanel>
        </Grid>
    </darkTheme:SfAcrylicPanel>
        </Grid>
</UserControl>

