<UserControl
    x:Class="WileyWidget.WinUI.Views.Panels.DashboardPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WileyWidget.WinUI.ViewModels.Main"
    mc:Ignorable="d">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20">
            <TextBlock
                Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                FontSize="32"
                FontWeight="Bold"
                HorizontalAlignment="Center" />

            <TextBlock
                Text="Welcome to the Wiley Widget Dashboard"
                FontSize="16"
                HorizontalAlignment="Center" />

            <!-- Loading Indicator -->
            <ProgressRing
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Width="40"
                Height="40"
                HorizontalAlignment="Center" />

            <!-- Dashboard Items -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="20"
                Width="600">
                <StackPanel Spacing="16">
                    <TextBlock
                        Text="Quick Actions"
                        FontSize="20"
                        FontWeight="SemiBold" />

                    <ItemsControl ItemsSource="{x:Bind ViewModel.DashboardItems, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:DashboardItemDisplay">
                                <Border
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    CornerRadius="4"
                                    Padding="16"
                                    Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock
                                            Text="{x:Bind Title}"
                                            FontSize="16"
                                            FontWeight="SemiBold" />
                                        <TextBlock
                                            Text="{x:Bind Description}"
                                            FontSize="14"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Margin="0,4,0,0" />
                                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                            <TextBlock
                                                Text="{x:Bind Status}"
                                                FontSize="12"
                                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                            <TextBlock
                                                Text="{x:Bind Count}"
                                                FontSize="12"
                                                FontWeight="Bold" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button
                        Content="Reload Dashboard"
                        Command="{x:Bind ViewModel.LoadDashboardCommand}"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
