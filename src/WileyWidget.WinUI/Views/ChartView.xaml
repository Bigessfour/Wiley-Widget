<Page
    x:Class="WileyWidget.WinUI.Views.ChartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Charts &amp; Analytics" 
                   Style="{ThemeResource TitleTextBlockStyle}" 
                   Margin="24,24,24,12" />

        <Grid Grid.Row="1" Margin="24,0,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    BorderThickness="1"
                    Padding="12"
                    Margin="0,0,0,12">
                <lvc:CartesianChart Series="{x:Bind ViewModel.ChartSeries, Mode=OneWay}"
                                   XAxes="{x:Bind ViewModel.XAxes}"
                                   YAxes="{x:Bind ViewModel.YAxes}"
                                   AutomationProperties.Name="Main Chart" />
            </Border>

            <Border Grid.Row="1"
                    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    BorderThickness="1"
                    Padding="12">
                <lvc:PieChart Series="{x:Bind ViewModel.PieChartSeries, Mode=OneWay}"
                             AutomationProperties.Name="Category Distribution Chart" />
            </Border>
        </Grid>

        <ProgressRing Grid.Row="1"
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      Width="50"
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center" />
    </Grid>
</Page>
