<Window
    x:Class="WileyWidget.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WileyWidget.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar Area -->
        <Border Grid.Row="0" Background="#0078D4" Padding="16,12">
            <TextBlock Text="Wiley Widget - WinUI 3 Shell" 
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="White"/>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="1" Padding="16">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
                <TextBlock Text="Welcome to WileyWidget WinUI 3" 
                           FontSize="24"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="StatusText"
                           Text="Project initialized successfully"
                           HorizontalAlignment="Center"
                           FontSize="14"/>
                <Button x:Name="myButton" 
                        Content="Click Me" 
                        Click="myButton_Click"
                        HorizontalAlignment="Center"
                        Padding="24,8"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
