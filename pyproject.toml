[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wiley-widget"
version = "0.1.0"
dependencies = [
    "requests>=2.25.0",
    "pandas>=1.3.0"
]

[tool.black]
line-length = 88

[tool.mypy]
strict = true
