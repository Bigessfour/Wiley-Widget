# Seed coverage report — 2025-12-25

Summary:

- Verified which database sets are seeded via migrations (OnModelCreating.HasData) and which are populated at runtime by `DataSeedingService`.
- Updated `DataSeedingService` to require migrations for relational providers (avoid `EnsureCreated` for SQL Server).

Seed coverage:

- MunicipalAccounts: Seeded (HasData in AppDbContext)
- Departments: Seeded (HasData in AppDbContext)
- BudgetEntries: Partially seeded (HasData migration + runtime seeder creates additional sample entries)
- Funds: Seeded (HasData in AppDbContext)
- Transactions: No explicit seed (runtime test data may insert)
- Enterprises: No explicit seed
- AppSettings: Seeded (HasData in AppDbContext)
- FiscalYearSettings: No explicit seed
- UtilityCustomer: No explicit seed
- UtilityBill: No explicit seed
- Charge: No explicit seed
- BudgetPeriods: Seeded (HasData) and runtime seeder ensures one exists
- Invoice: No explicit seed
- Vendor: Seeded (HasData)
- AuditEntries: No default seed (audit rows are created at runtime during operations)
- TaxRevenueSummaries: Seeded (HasData)
- ActivityLogs: No explicit seed
- DepartmentCurrentCharges: No explicit seed
- DepartmentGoals: No explicit seed
- AIInsights: No explicit seed
- QBMappingConfigurations: No explicit seed (new table; migrations added)

Notes & recommendations:

- Most lookup/lookup-like entities (Departments, Funds, Vendors, BudgetPeriods, MunicipalAccounts, AppSettings, TaxRevenueSummaries) are seeded via migrations (`HasData`). This ensures CI/dev DBs created from migrations have required lookups.
- `DataSeedingService` provides idempotent runtime seeding for sample `BudgetEntries`, and can be invoked via `--seed-data` or automatically during startup health check when DB is empty.
- I updated `DataSeedingService` to refuse seeding when relational providers have pending migrations. Run `dotnet ef database update` first to avoid inconsistent schemas.

Next steps you may want:

- Run a schema vs snapshot diff to find any out-of-sync tables (I can produce this report).
- Add lightweight HasData seeds for any additional small lookup tables you want guaranteed in all environments (e.g., FiscalYearSettings, Enterprises).
- Consider a documented `--seed-data --force` workflow for CI/dev to create reproducible test data.

If you want, I can now:

- Produce a schema diff between the current model snapshot and the development database.
- Add explicit idempotent seeding for specific missing tables (pick which ones).

-- Automated report generated by GitHub Copilot (GPT-5 mini)
