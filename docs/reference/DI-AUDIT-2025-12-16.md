# Dependency Injection Audit (2025-12-16)

**Status:** Updated (2026-01) - JARVIS Migration Complete

## 🛠️ Service Registrations

### Core Services
- [x] `IActivityLogRepository` -> `ActivityLogRepository`
- [x] `IAppPreferencesService` -> `AppPreferencesService`
- [x] `IXAIService` -> `XAIService` (Streaming support verified)
- [x] `IJARVISPersonalityService` -> `JARVISPersonalityService`

### UI Host Services
- [x] `JARVISChatHostForm` (Singleton/Transient as required by Modal flow)
- [x] `IChatBridgeService` -> `ChatBridgeService` (Blazor-to-C# bridge)

## ❌ Removed Components (Cleaned)
The following components have been removed from the DI container and the project:
- [x] `ChatPanel` / `AIChatControl` (WinForms)
- [x] `ChatPanelViewModel` (Legacy)
- [x] `IConversationRepository` (If no longer used by Blazor version)

## 📋 Post-Migration Checklist
- [x] Ensure `JARVISChatHostForm` is correctly resolved by `RibbonFactory`.
- [x] Verify `BlazorWebView` initialization doesn't block the UI thread.
- [ ] Add unit tests for `ChatBridgeService` response handling.
