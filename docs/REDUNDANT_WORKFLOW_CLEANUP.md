# Redundant Workflow Files - Cleanup Guide\n\n## Summary\n\nTwo GitHub Actions workflow files in `.github/workflows/` are redundant and should be removed:\n\n1. **`restore.yml`** — Only runs NuGet restore (superseded by build-winforms.yml)\n2. **`dotnet.yml`** — Generic build/test/coverage (superseded by build-winforms.yml)\n\nBoth are covered by the comprehensive `build-winforms.yml` workflow.\n\n---\n\n## Detailed Analysis\n\n### 1. `restore.yml` — REDUNDANT\n\n**Current behavior**:\n```yaml\nname: Restore CI\non:\n  pull_request:\n    branches: [\"main\"]\n  push:\n    branches: [\"main\"]\n\njobs:\n  restore:\n    runs-on: windows-latest\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n      - name: Setup .NET\n        uses: actions/setup-dotnet@v4\n        with:\n          dotnet-version: 10.0.x\n      - name: Restore solution\n        run: dotnet restore WileyWidget.sln\n```\n\n**Why redundant**:\n- `build-winforms.yml` already runs `dotnet restore WileyWidget.sln` as a step\n- Separate restore job adds no value (it's part of every build)\n- Consumes GitHub Actions minutes for duplicate work\n\n**Action**: DELETE\n\n---\n\n### 2. `dotnet.yml` — REDUNDANT\n\n**Current behavior**:\n```yaml\nname: .NET Build & Test\non:\n  push:\n    branches: [main, upgrade-to-NET10]\n  pull_request:\n    branches: [main]\n\njobs:\n  build-test:\n    runs-on: windows-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Setup .NET\n        uses: actions/setup-dotnet@v4\n        with:\n          dotnet-version: 10.0.x\n      - name: Restore\n        run: dotnet restore\n      - name: Build\n        run: dotnet build --no-restore -c Release\n      - name: Test\n        run: dotnet test --no-build -c Release\n      - name: Upload coverage (optional)\n        uses: codecov/codecov-action@v4\n```\n\n**Why redundant**:\n- `build-winforms.yml` performs the same workflow (restore → build → test → coverage)\n- `dotnet.yml` targets .NET 10.0.x (potential version mismatch with rest of build)\n- Tests are run more comprehensively in `build-winforms.yml` (filtered tests, multiple test projects)\n- `build-winforms.yml` publishes artifacts (missing from `dotnet.yml`)\n\n**Action**: DELETE\n\n---\n\n## Workflow Selection Matrix\n\nAfter cleanup, you'll have:\n\n| Workflow | Trigger | Purpose | Run Time |\n|----------|---------|---------|----------|\n| **fast-pr-feedback.yml** | PR open/update | Quick linting & tests (PRs) | ~2 min |\n| **build-winforms.yml** | Push to main/develop | Full build, test, coverage, publish | ~45 min |\n| **syncfusion-theming.yml** | WinForms changes | Theming compliance + MCP validation | ~10 min |\n| **e2e-headless-mcp.yml** | Manual dispatch | UI E2E tests (on-demand) | ~30 min |\n| **startup-ci.yml** | Pushes (conditional) | Startup smoke test | ~5 min |\n| **logging-evaluation.yml** | Manual dispatch | Logging diagnostic (on-demand) | ~15 min |\n\n---\n\n## Cleanup Steps\n\n### Option A: Git (Recommended)\n\n```bash\n# Navigate to repo root\ncd ~/path/to/WileyWidget\n\n# Remove files from git\ngit rm .github/workflows/restore.yml .github/workflows/dotnet.yml\n\n# Verify removal\ngit status\n# Output should show:\n# deleted:    .github/workflows/restore.yml\n# deleted:    .github/workflows/dotnet.yml\n\n# Commit\ngit commit -m \"chore: remove redundant workflow files (superseded by build-winforms.yml)\"\n\n# Push\ngit push origin main\n```\n\n### Option B: Manual (If git not available)\n\n1. Go to repository on GitHub\n2. Navigate to `.github/workflows/`\n3. Click on `restore.yml` → Select **Delete this file** → Commit\n4. Click on `dotnet.yml` → Select **Delete this file** → Commit\n\n---\n\n## Verification\n\nAfter cleanup:\n\n```bash\n# Verify files are gone\nls -la .github/workflows/\n\n# Should show:\n# build-winforms.yml ✅\n# e2e-headless-mcp.yml ✅\n# fast-pr-feedback.yml ✅\n# logging-evaluation.yml ✅\n# startup-ci.yml ✅\n# syncfusion-theming.yml ✅\n# (restore.yml and dotnet.yml should NOT appear)\n```\n\n### Monitor GitHub Actions\n\n1. Go to repository → **Actions** tab\n2. Verify that:\n   - No new jobs appear for \"Restore CI\" or \".NET Build & Test\"\n   - `fast-pr-feedback.yml` still runs on PR\n   - `build-winforms.yml` still runs on push to main\n   - `syncfusion-theming.yml` still runs on WinForms changes\n\n---\n\n## Why Safe to Delete\n\n✅ **No critical functionality**: Both redundant workflows only do restore/build/test, all covered by build-winforms.yml  \n✅ **No unique triggers**: No special branch or path triggers not already covered  \n✅ **No artifact uploads**: Not publishing anything unique  \n✅ **No deployments**: Not used in any deployment pipeline  \n✅ **No notifications**: Not configured with special alerts or webhooks  \n✅ **Version compatible**: build-winforms.yml uses .NET 9.0.x (matches project)  \n\n---\n\n## Side Effects: NONE\n\nRemoving these files will:\n- ✅ **Reduce GitHub Actions minutes** (no duplicate restore/build jobs)\n- ✅ **Reduce CI noise** (cleaner workflow list)\n- ✅ **Improve clarity** (single source of truth: build-winforms.yml)\n- ❌ **Break nothing** (all workflows are superseded)\n\n---\n\n## Timeline\n\n- **Immediately**: Safe to delete (no dependencies)\n- **Recommended**: Delete as part of next batch of CI/CD improvements\n- **Not time-critical**: No blocking dependencies\n\n---\n\n## Notes\n\n- If you added these workflows for a specific reason (e.g., testing on .NET 10), let me know and we can keep them\n- The `upgrade-to-NET10` branch trigger in `dotnet.yml` suggests it was for transition work — verify this branch no longer exists before deleting\n- All current functionality is preserved in active workflows\n\n---\n\n**Document Generated**: 2026-01-08  \n**Recommendation**: SAFE TO DELETE\n