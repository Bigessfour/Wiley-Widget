<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net9.0-windows</TargetFramework>
        <IsPackable>false</IsPackable>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- Tests temporarily disabled per request. This project kept but stripped of test
         packages and project references so it won't run or fail when running tests.
         If you prefer deletion instead of a stub, say so and I will remove the
         entire folder. -->

</Project>