node_modules
**/bin
**/obj
.vscode
.trunk
coverage-report
publish
**/*.png
**/*.jpg
**/*.gif
**/*-lock.json
**/*.min.*

# Workflows sometimes include non-YAML constructs or code fences that Prettier's YAML parser can choke on.
# We previously excluded workflow files to work around parse errors — now we want to enable them.
# (Removed excludes for .github/workflows so Prettier will check/format workflow YAML files.)
